#!/bin/bash

set -e

tagStart=$(expr index "$IMAGE_NAME" :)
repoName=${IMAGE_NAME:0:tagStart-1}
tool=$(echo ${repoName} | awk -F/ '{print $2}' )

cd ${tool}/${DOCKER_TAG}

docker build -t ${repoName}:${DOCKER_TAG} .
docker push ${repoName}:${DOCKER_TAG}